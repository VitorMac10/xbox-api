version: '3'

services:
  producer:
    build:
      context: ./producer
      dockerfile: Dockerfile
    working_dir: /usr/src/app
    networks:
      - app-network
    links:
      - rabbitmq

  consumer:
    build:
      context: ./consumer
      dockerfile: Dockerfile
    working_dir: /usr/src/app
    networks:
      - app-network
    links:
      - rabbitmq

  rabbitmq:
    image: rabbitmq:3.8-alpine
    networks:
      - app-network

networks:
  app-network:
    driver: bridge